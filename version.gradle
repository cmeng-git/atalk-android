allprojects {
    ext {
        // When using dynamic versions for those, do *not* use [1.0, 2.0), since this will
        // also pull in 2.0-alpha1. Instead use [1.0, 1.0.99].
        // See also: https://issues.apache.org/jira/browse/MNG-6232

        minSdkVersion = 19
        compileSdkVersion = 28
        buildToolsVersion = '29.0.2'
        supportLibraryVersion = '1.0.0'
        gmsPlayServicesVersion = '17.0.0'
    }
}